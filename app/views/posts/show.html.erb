<h1>RecipeBook Book</h1>

<%= @post.message %>

<h2>Add a comment:</h2>
<%= form_with(model: [ @post, @post.comments.build ], local: true) do |form| %>
  <p>
    <%= form.text_area :body %>
  </p>
  <p>
   <%= form.submit %>  </p>
<% end %>

<%= link_to posts_url do %>
  Back to  posts
<% end %>
